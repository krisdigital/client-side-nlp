# Client side nlp

Testing client side nlp using transformers.js on webgpu. To run start the dummy backend 

```
cd dummy-backend
go run main.go
```

and the frontend

```
cd frontend
pnpm run dev
```

You can change between two pages by switching between 2 and 3 in the "fake" address bar to load different datasets (`/data/2.json`).
